mixin videos(video)
    div 
        h4=video.title
        ul
            li  #{video.likes}
            li #{video.comments}
            li Posted #{video.createdAt}. 
            li #{video.views} views.
